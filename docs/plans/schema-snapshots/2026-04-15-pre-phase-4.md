# Pre-Phase-4 Schema Snapshot

**Date:** 2026-04-15
**Supabase project:** otikhvpmjijwgnabxspd
**Captured by:** Phase 4 executor, Task 1 Step 5
**Branch:** v2-proposal-generator (after Phase 3 complete)

## `is_org_member()` helper

Exists as `public.is_org_member(p_org_id uuid) RETURNS boolean`. Note the argument name is `p_org_id` (prefix `p_`), not `org_uuid` as the plan example used — the Phase 4 migration SQL must reference `public.is_org_member(...)` positionally so the name doesn't matter at the call site.

## `public.flowsheets` columns (pre-migration)

| column | type | nullable |
|---|---|---|
| id | uuid | NO |
| project_id | uuid | NO |
| name | text | NO |
| graph_data | jsonb | NO |
| discharge_standards | jsonb | YES |
| created_at | timestamptz | YES |
| updated_at | timestamptz | YES |

No `proposal_data` column.

## `public.profiles` columns (pre-migration)

| column | type | nullable |
|---|---|---|
| id | uuid | NO |
| full_name | text | YES |
| company | text | YES |
| role | text | YES |
| subscription_tier | text | YES |
| created_at | timestamptz | YES |
| stripe_customer_id | text | YES |
| stripe_subscription_id | text | YES |
| subscription_period_end | timestamptz | YES |

No `company_logo_url` or `designer_title` columns. `company` exists — reused (no new `company_name`).

## Pre-existing AquaSim tables

Phase 4 will add `boq_line_items` and `project_proposals`. Verified zero rows in `information_schema.tables` for both before applying migrations.

## Existing migrations (from list_migrations MCP call)

1. 20250328090816 violet_star
2. 20250328091651 rapid_heart
3. 20250328092035 little_scene
4. 20260401204605 create_biowin_schema
5. 20260401211026 add_share_links_and_stripe_fields
6. 20260412115650 fdr_initial_schema
7. 20260412115844 fdr_storage_bucket

Phase 4 will add 3 more, bringing the count to 10.
